pub fn run(input: &str, part: u8) -> String {
    "2025 4".into()
}

#[cfg(test)]
mod test {
    use super::*;

    #[test]
    fn test_run() {
        let input = "";
        assert_eq!("", &run(input, 1));
    }
}
